export const loginUser = (credentials, history, setFieldError, setSubmitting) => {
    const user = {
      name: "Nifri",
      "email": "nifri@gmail.com"
    }
    const status = true;

    if (status === true){
    
    }else{
    }
}

export const registerUser = (credentials, history, setFieldError, setSubmitting) => {

}

export const logoutUser = () => {

}
